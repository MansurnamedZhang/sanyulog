{
  "name": "process-log",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/notebook.test.mjs",
    "check": "node --check static/app.js && node --check static/notebook.js && node --check static/notebook-core.mjs",
    "format": "prettier --write static/*.js static/*.mjs static/*.css static/*.html tests/*.mjs",
    "format:check": "prettier --check static/*.js static/*.mjs static/*.css static/*.html tests/*.mjs"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
