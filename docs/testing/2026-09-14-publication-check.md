# 发布内容检查 · 2026-09-14

检查范围为 Git 暂存文件，涵盖源码、文档、测试、依赖锁文件、第三方资源和 Logo 元数据。本地数据目录未纳入提交。

- 未发现真实服务凭据、访问令牌、私钥、个人账号路径或实际服务器地址。
- 测试中的局域网 IP 已替换为保留示例域名 `process-log.example`。
- GitHub Actions 的 `test_only` 是临时测试数据库的固定测试值，不是正式部署凭据。
- `localhost`、回环地址、标准示例域名和公开依赖链接属于功能或测试内容。
- Logo 包含 C2PA 生成来源及公共证书信息；未发现用户姓名、账号路径或位置元数据，保留来源信息。
- 第三方许可证及版权归属保留。
- 数据库、附件、备份、环境文件、依赖目录和本地缓存由忽略规则排除。

方法包括暂存文件枚举、常见凭据格式与个人路径扫描、地址复核和 PNG 元数据检查。此检查未发现敏感或个人信息，但不能作为对所有未知秘密格式的绝对保证；后续新增文件仍需检查。
