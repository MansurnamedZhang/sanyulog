# Third-party notices

## KaTeX

The bundled `static/vendor/katex.mjs` renders LaTeX formulas offline.
Version and upstream attribution are included in `static/vendor/`.
KaTeX is distributed under the MIT license; retain its bundled license when redistributing this application.

## Python dependencies

The optional PostgreSQL backend uses Psycopg, pinned in `requirements.txt`.
Its upstream license and the licenses of its binary dependencies apply separately.

## Development dependencies

Prettier is a development-only formatter distributed under the MIT license.
It is not required to run Process Log.

## html-to-image 1.11.13
MIT License. Source: https://github.com/bubkoo/html-to-image
Vendored browser bundle: static/vendor/html-to-image.js; license: static/vendor/html-to-image.LICENSE.

## Tiptap visual Markdown editor

Tiptap 3.31.3 (MIT), ProseMirror, Marked and KaTeX are bundled locally into `static/vendor/rich-editor.js`. Exact dependency versions are pinned in `package-lock.json`; full bundled runtime license texts are in `static/vendor/rich-editor.LICENSE.txt`. Source: https://github.com/ueberdosis/tiptap . The bundle is generated by esbuild (MIT; development only).
